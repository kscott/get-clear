[project]
name = "get-clear-mcp"
version = "0.1.0"
description = "MCP server for the Get Clear CLI suite"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
