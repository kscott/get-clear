name: Tagged Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build release binary
        run: swift build -c release --product contacts-bin

      - name: Rename binary
        run: cp .build/release/contacts-bin contacts-bin

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: |
            ## Install

            1. Download `contacts-bin` below
            2. Move it to `~/bin/contacts-bin` (create `~/bin` if needed)
            3. Make it executable: `chmod +x ~/bin/contacts-bin`
            4. On first run, macOS will prompt for Contacts access

            See the [README](https://github.com/kscott/contacts-cli#readme) for full setup instructions.
          files: contacts-bin
          make_latest: true
